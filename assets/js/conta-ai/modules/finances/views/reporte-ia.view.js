export function renderReporteIaView() {
  return `
    <div class="eyebrow">Reporte IA</div>
    <h2>Preparación de reporte para IA</h2>
    <p class="muted">Preparará contexto financiero validado para que IA pueda explicar indicadores, detectar posibles errores, generar reportes y responder preguntas.</p>
    <span class="status soon">Pendiente de implementación</span>
    <div class="notice">Esta vista todavía no conecta IA ni envía datos. La IA no realizará cálculos contables principales.</div>
  `;
}
